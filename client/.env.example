# Full base URL of the backend API (no trailing slash).
# Example for local dev with proxy disabled:
# VITE_API_URL=http://localhost:5000/api
# If omitted, dev server will proxy /api to the backend.
VITE_API_URL=

# Optional: Vite base path when deploying under a sub-path
# BASE_URL=/
